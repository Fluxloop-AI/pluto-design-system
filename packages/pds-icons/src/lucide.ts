/**
 * lucide-react 전량 재노출. 트리 셰이킹에 안전.
 * 개별 아이콘 임포트:
 *   import { ChevronDown, Search } from "@pluto/pds-icons/lucide";
 *
 * PDS 기본 크기·스타일이 적용된 wrapper 가 필요하면 Icon 프리미티브(Phase 1) 사용.
 */

export type { LucideIcon, LucideProps } from "lucide-react";
export * from "lucide-react";
