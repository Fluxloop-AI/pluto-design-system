/**
 * @fluxloop-ai/pds-ui — 소스 노출형 컴포넌트 패키지.
 *
 * 1차 소비 경로: shadcn CLI 로 앱 레포에 파일 복사.
 *   npx shadcn add https://pds.pluto.com/r/button
 *
 * 2차 소비 경로: 워크스페이스 내부(docs 앱, 동 모노레포 앱)에서는 소스 import.
 *   import { Button } from "@fluxloop-ai/pds-ui/components/button";
 *
 * 컴포넌트는 Phase 1 부터 src/components/ 에 추가된다.
 */
export {};
